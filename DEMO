--[[
    Ahmed Hub | CLAN ZERO EDITION
    REMASTERED BY: Taraf69788
    ROBLOX: zdrkn305
]]

-- منع التكرار وضمان استقلالية السكربت
if _G.AhmedLoaded then return end
_G.AhmedLoaded = true

-- استدعاء المكتبة الاحترافية التي تستخدمها ALFA (نفس الواجهة الخضراء)
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("Ahmed Hub | CLAN ZERO", "DarkTheme")

-- [ قسم الحقوق - معدل بالكامل ]
local CreditsTab = Window:NewTab("الحقوق")
local CreditsSection = CreditsTab:NewSection("المطور: Taraf69788")

-- ميزة RGB الاحترافية للاسم (تغيير الألوان)
local WelcomeLabel = CreditsSection:NewLabel("Welcome to ZERO HUB")
spawn(function()
    while task.wait() do
        local color = Color3.fromHSV(tick() % 5 / 5, 1, 1)
        WelcomeLabel:UpdateColor(color)
    end
end)

CreditsSection:NewButton("حسابي على روبلوكس: Taraf69788", "نسخ اليوزر", function()
    setclipboard("zdrkn305")
end)

-- [ قسم التخريب المتقدم - ميزات ALFA الأصلية ]
local ChaosTab = Window:NewTab("التخريب")
local ChaosSection = ChaosTab:NewSection("ميزات Brookhaven القوية")

ChaosSection:NewButton("سرقة بنك برووكهافن تلقائياً", "Auto Rob Bank", function()
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-31.6, 11.2, -156.9)
    fireclickdetector(game:GetService("Workspace").Bank.Safe.ClickDetector)
end)

ChaosSection:NewButton("فتح جميع أبواب السيرفر", "Unlock Server Doors", function()
    for _, v in pairs(game:GetService("Workspace").Houses:GetDescendants()) do
        if v:IsA("ClickDetector") then fireclickdetector(v) end
    end
end)

-- [ قسم السكنات - VIP ]
local SkinsTab = Window:NewTab("السكنات")
local SkinSection = SkinsTab:NewSection("تعديل المظهر - كلان زيرو")

SkinSection:NewButton("تفعيل سكن ديمو الأصلي", "Load VIP Skin", function()
    -- الكود الداخلي لتحويل السكن (نفس ميزة ALFA)
end)

-- [ قسم الشات والأغاني ]
local ExtraTab = Window:NewTab("إضافات مجانية")
local ExtraSection = ExtraTab:NewSection("Clan ZERO Support")

ExtraSection:NewButton("سبام كلان زيرو في الشات", "Zero On Top", function()
    game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer("CLAN ZERO ON TOP!", "All")
end)

-- إشعار التشغيل الفخم
Library:Notify("Ahmed Hub | ZERO", "تم تشغيل السكربت بنجاح يا Taraf69788", 5)
